//! Handles listening of a connected process.

/// Info and logic about the receiving side of a connection with a process.
pub struct EndpointListener {
    /// The id of the connection.
    id: u64,
    //TODO: Add a sender to uinput and input.
}

